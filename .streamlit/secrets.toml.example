# ─────────────────────────────────────────────
# .streamlit/secrets.toml  — LEARNIX DTE HUB
# Copia este archivo como secrets.toml y rellena los valores.
# NUNCA subas secrets.toml a git.
# ─────────────────────────────────────────────

# Supabase — obtenlos en: Project Settings → API
SUPABASE_URL = "https://tprgesgphfazlninmfsj.supabase.co"
SUPABASE_KEY = "eyJ..."   # anon/public key (no la service role)

# Gemini — solo servidor, nunca en UI
[gemini]
api_key = "AIza..."
