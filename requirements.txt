streamlit>=1.32.0
pdfplumber>=0.10.3
pandas>=2.0.0
openpyxl>=3.1.2
Pillow>=10.0.0
requests>=2.28.0
# Evitar conflicto con pyiceberg (presente en Streamlit Cloud)
rich>=10.11.0,<15.0.0
# QR reader + render de PDF a imagen para la capa de visión IA (DTEs del MH El Salvador)
PyMuPDF>=1.23.0
pyzbar
# Plan C - OCR Tesseract para UUID en PDFs escaneados sin QR (packages.txt: tesseract-ocr)
pytesseract>=0.3.1
# IA — Groq Cloud (fallback): texto (llama-3.3-70b-versatile) + visión (llama-4-scout-17b)
groq>=0.9.0
# IA — Vertex AI Express (motor prioritario): gemini-2.0-flash, JSON estructurado
google-genai>=1.0.0
